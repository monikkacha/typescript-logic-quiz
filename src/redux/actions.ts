import { BsPaypal } from "react-icons/bs";
import { INCREASE_POINT, NEXT_QUESTION } from "./redux_constatns";

export const NextQuestion = {
    type: NEXT_QUESTION
}

export const IncreasePoint = {
    type: INCREASE_POINT
}